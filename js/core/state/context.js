/**
 * State Context
 * Defines the React context for application state
 */

import React from 'react';

// Create context for application data
const DataContext = React.createContext(null);

export default DataContext;